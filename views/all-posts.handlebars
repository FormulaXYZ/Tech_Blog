<div class="posts" >
    {{#each posts}}
<article class="card">
<div class="card-headre flex-row justify-space-between">
    <h2>
        <a href="/post/{{id}}">{{title}}</a>
    </h2>
</div>
    bloger is {{user.name}} on {{format_date createdAt}}</div>
   
   </div>
   <div class="card-body">{{body}}
   </div>
    </article>
    {{/each}}
</div>